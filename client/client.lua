RegisterNetEvent('restorePlayerData')
AddEventHandler('restorePlayerData', function(userData)
    -- For example, you can restore the player's money and items based on the data received

    -- Exemple :
    -- money = userData.money
    -- items = userData.items

    -- You can then use this data to update the player's status on the client side, such as updating the display of money and items in the UI.
end)
